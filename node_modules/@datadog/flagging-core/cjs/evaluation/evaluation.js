"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.evaluateRulesBasedConfiguration = evaluateRulesBasedConfiguration;
const time_1 = require("../time");
const errors_1 = require("./errors");
const evaluateForSubject_1 = require("./evaluateForSubject");
const evaluationMetadata_1 = require("./evaluationMetadata");
function evaluateRulesBasedConfiguration(config, type, flagKey, defaultValue, context, logger) {
    const evaluationTimestampMs = (0, time_1.timeStampNow)();
    if (!config) {
        return {
            value: defaultValue,
            reason: 'ERROR',
            errorCode: 'PROVIDER_NOT_READY',
            flagMetadata: (0, evaluationMetadata_1.createEvaluationTimestampMetadata)(evaluationTimestampMs),
        };
    }
    const { targetingKey: subjectKey, ...remainingContext } = context;
    // Include the subjectKey as an "id" attribute for rule matching only when present
    const subjectAttributes = {
        ...(subjectKey != null ? { id: subjectKey } : {}),
        ...remainingContext,
    };
    const flag = config.flags[flagKey];
    if (!flag) {
        logger.debug('returning default value because flag is not found', { flagKey, subjectKey });
        return {
            value: defaultValue,
            reason: 'ERROR',
            errorCode: 'FLAG_NOT_FOUND',
            flagMetadata: (0, evaluationMetadata_1.createEvaluationTimestampMetadata)(evaluationTimestampMs),
        };
    }
    try {
        return (0, evaluateForSubject_1.evaluateForSubject)(flag, type, subjectKey, subjectAttributes, defaultValue, logger, evaluationTimestampMs);
    }
    catch (error) {
        if (error instanceof errors_1.TargetingKeyMissingError) {
            return {
                value: defaultValue,
                reason: 'ERROR',
                errorCode: 'TARGETING_KEY_MISSING',
                flagMetadata: (0, evaluationMetadata_1.createEvaluationTimestampMetadata)(evaluationTimestampMs),
            };
        }
        logger.error('Error evaluating flag', { error });
        return {
            value: defaultValue,
            reason: 'ERROR',
            errorCode: 'GENERAL',
            flagMetadata: (0, evaluationMetadata_1.createEvaluationTimestampMetadata)(evaluationTimestampMs),
        };
    }
}
//# sourceMappingURL=evaluation.js.map